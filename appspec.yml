version: 0.0
os: ubuntu
files:
  - source: .
    destination: /home/ubuntu/motherfucker/discord-sam-jackson-bot
hooks:
  AfterInstall:
   - location: setup.sh
     timeout: 300
     runas: root
  ApplicationStart:
   - location: run.sh
     timeout: 300
     runas: root
